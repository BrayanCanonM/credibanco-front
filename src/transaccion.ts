export interface Transaccion{
    idTransaccion: string;
    pan: string;
    numReferencia: string;
    totalCompra: string;
    direccionCompra: string;
    estadoCompra: string;
    fecha: string;
}